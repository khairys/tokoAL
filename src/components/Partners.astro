---
// Brands & Community Section Component
import logoPacific from '../assets/partners/Pacific Bike Logo vector (_cdr).jpg';
import logoElement from '../assets/partners/elementlogo.jpeg';
import logoUnited from '../assets/partners/united logo.png';
import logoFamily from '../assets/partners/family logo.png';
import logoPmb from '../assets/partners/pmb logo.png';
import logoKenda from '../assets/partners/kenda logo.jpg';
import logoSwallow from '../assets/partners/swallow logo.png';
import logoRevo from '../assets/partners/revo logo.jpg';

const brands = [
    { name: 'Pacific Bike', logo: logoPacific, sizeClass: 'max-h-10 sm:max-h-16' },
    { name: 'United Bike', logo: logoUnited, sizeClass: 'max-h-8 sm:max-h-11' },
    { name: 'Element Bike', logo: logoElement, sizeClass: 'max-h-8 sm:max-h-11' },
    { name: 'Family Bike', logo: logoFamily, sizeClass: 'max-h-10 sm:max-h-16' },
    { name: 'PMB Toys', logo: logoPmb, sizeClass: 'max-h-10 sm:max-h-16' },
    { name: 'Kenda Tire', logo: logoKenda, sizeClass: 'max-h-8 sm:max-h-11' },
    { name: 'Swallow Deli', logo: logoSwallow, sizeClass: 'max-h-7 sm:max-h-11' },
    { name: 'Revo Parts', logo: logoRevo, sizeClass: 'max-h-10 sm:max-h-16' },
];

const communities = [
    {
        name: 'Gorikat',
        category: 'Gowes Kompak & Rutin',
        initials: 'GK',
        gradient: 'from-[#116530] to-primary',
        icon: 'fa-bicycle',
    },
    {
        name: 'East Cycle',
        category: 'Roadbike & Urban',
        initials: 'EC',
        gradient: 'from-secondary to-primary',
        icon: 'fa-road',
    },
    {
        name: 'Desavora',
        category: 'MTB & Adventure',
        initials: 'DV',
        gradient: 'from-emerald-700 to-green-600',
        icon: 'fa-mountain',
    },
    {
        name: 'Ciruas Bike Sport',
        category: 'All-Terrain Cycling',
        initials: 'CBS',
        gradient: 'from-amber-600 to-orange-500',
        icon: 'fa-compass',
    },
];

const sponsorWaUrl = "https://api.whatsapp.com/send?phone=6281996535525&text=" + encodeURIComponent("Halo Toko AL! 👋 Saya ingin mengajukan kerjasama / sponsorship event untuk komunitas kami. Mohon informasi terkait prosedur dan persyaratannya. Terima kasih!");
---

<!-- === BRANDS & COMMUNITY SECTION === -->
<section id="partners" class="py-20 bg-white relative overflow-hidden border-t border-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        
        <!-- ============================================== -->
        <!-- 1. BRANDS SECTION (DI ATAS)                    -->
        <!-- ============================================== -->
        <div class="text-center mb-14 fade-in-up">
            <h2 class="text-3xl sm:text-4xl font-extrabold text-gray-900 tracking-tight">
                Brand yang Tersedia di Toko AL
            </h2>
            <div class="w-24 h-1.5 bg-gradient-to-r from-[#116530] via-primary to-secondary mx-auto mt-4 rounded-full"></div>
            <p class="mt-4 text-gray-600 max-w-2xl mx-auto text-base sm:text-lg leading-relaxed">
                Pilihan produk dari berbagai brand yang tersedia di Toko AL.
            </p>
        </div>

        <!-- Brands Grid (Floating Clean Logos, Tanpa Kotak Kontainer, Warna Asli Full) -->
        <div class="grid grid-cols-2 sm:grid-cols-4 gap-6 sm:gap-12 md:gap-14 items-center justify-items-center max-w-5xl mx-auto px-4 fade-in-up">
            {brands.map((brand) => (
                <div 
                    class="flex items-center justify-center h-14 sm:h-20 w-full transition-transform duration-200 hover:scale-105 cursor-default"
                    title={brand.name}
                >
                    <img
                        src={brand.logo.src}
                        alt={`Logo ${brand.name}`}
                        class={`${brand.sizeClass} max-w-full object-contain drop-shadow-[0_1px_2px_rgba(0,0,0,0.04)] origin-center transition-transform`}
                        loading="lazy"
                    />
                </div>
            ))}
        </div>

        <!-- ============================================== -->
        <!-- DIVIDER PEMISAH ANTAR SECTION                   -->
        <!-- ============================================== -->
        <div class="my-16 sm:my-20 max-w-4xl mx-auto">
            <div class="border-t border-gray-100"></div>
        </div>

        <!-- ============================================== -->
        <!-- 2. COMMUNITY SECTION (DI BAWAH)                -->
        <!-- ============================================== -->
        <div class="text-center mb-12 fade-in-up">
            <h2 class="text-3xl sm:text-4xl font-extrabold text-gray-900 tracking-tight">
                Tumbuh Bersama Komunitas
            </h2>
            <div class="w-24 h-1.5 bg-gradient-to-r from-[#116530] via-primary to-secondary mx-auto mt-4 rounded-full"></div>
            <p class="mt-4 text-gray-600 max-w-2xl mx-auto text-base sm:text-lg leading-relaxed">
                Kami mendukung dan berkolaborasi dengan komunitas serta kegiatan bersepeda di sekitar kami.
            </p>
        </div>

        <!-- Logo / Badge Komunitas Sementara (Mudah Diganti Tag <img> Logo Asli) -->
        <div class="grid grid-cols-2 sm:grid-cols-4 gap-6 sm:gap-8 max-w-4xl mx-auto fade-in-up">
            {communities.map((comm) => (
                <div class="flex flex-col items-center text-center group">
                    <!-- Placeholder Logo Badge (Tinggal ganti dengan <img src="..." alt={comm.name} class="w-20 h-20 object-contain" /> nanti) -->
                    <div class={`w-20 h-20 sm:w-24 sm:h-24 rounded-2xl bg-gradient-to-br ${comm.gradient} text-white flex flex-col items-center justify-center shadow-md shadow-primary/10 group-hover:scale-105 group-hover:shadow-lg transition-all duration-300 mb-3 relative overflow-hidden`}>
                        <i class={`fas ${comm.icon} text-xl sm:text-2xl mb-1 opacity-90`}></i>
                        <span class="text-xs sm:text-sm font-extrabold tracking-wider">{comm.initials}</span>
                    </div>
                    
                    <h3 class="font-bold text-base text-gray-900 group-hover:text-primary transition-colors">
                        {comm.name}
                    </h3>
                </div>
            ))}
        </div>

        <!-- Call to Action: Pengajuan Sponsorship (Langsung ke WhatsApp) -->
        <div class="mt-14 text-center max-w-xl mx-auto fade-in-up">
            <p class="text-base sm:text-lg font-bold text-gray-800 mb-5">
                Punya kegiatan atau komunitas yang ingin berkolaborasi dengan kami?
            </p>
            <a
                href={sponsorWaUrl}
                target="_blank"
                rel="noopener noreferrer"
                class="w-full sm:w-auto inline-flex items-center justify-center gap-2.5 px-8 py-4 bg-primary hover:bg-dark text-white font-bold text-sm sm:text-base rounded-full shadow-lg shadow-primary/25 hover:shadow-2xl hover:shadow-dark/30 transform hover:-translate-y-0.5 active:scale-95 transition-all duration-150 cursor-pointer group"
            >
                <i class="fab fa-whatsapp text-xl transition-transform duration-200 group-hover:scale-110"></i>
                <span>Ajukan Sponsorship</span>
            </a>
        </div>

    </div>
</section>
